    class="thumbnail modal-media" 
    data-modal-id="modal-media-{{@index}}"
    data-title="{{title}}"
    {{#when type eq img}}
        href="{{folder_base}}/{{fileName}}" 
        data-lightbox="set-{{@index}}" 
    {{/when}}
    {{#when extension eq pdf}} 
        href="#" 
        onclick="openMedia({{@index}})"
    {{/when}}
    {{#when type eq mov}}
        href="#" 
        onclick="openMedia({{@index}})" 
    {{/when}}
