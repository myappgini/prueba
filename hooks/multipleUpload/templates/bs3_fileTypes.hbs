    class="thumbnail modal-media" 
    data-modal-id="modal-media-{{@key}}"
    data-title="{{title}}"
    {{#when type eq img}}
        href="{{folder_base}}/{{fileName}}" 
        data-lightbox="set-{{@key}}" 
    {{/when}}
    {{#when extension eq pdf}} 
        href="#" 
        onclick="openMedia({{@key}})"
    {{/when}}
    {{#when type eq mov}}
        href="#" 
        onclick="openMedia({{@key}})" 
    {{/when}}
