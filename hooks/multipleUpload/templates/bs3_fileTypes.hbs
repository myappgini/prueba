    class="thumbnail modal-media" 
    data-modal-id="modal-media-{{@key}}"
    data-title="{{title}}"
    {{#when type eq img}}
        href="{{folder_base}}/{{fileName}}" 
        data-lightbox="{{#if ../gallery}}gallery{{else}}dv{{/if}}-set" 
    {{/when}}
    {{#when extension eq pdf}} 
        href="{{folder_base}}/{{fileName}}" 
        target="_blank"
    {{/when}}
    {{#when type eq mov}}
        href="#" 
    {{/when}}
